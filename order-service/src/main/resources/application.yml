spring:
    application:
        name: ORDER-SERVICE
    datasource:
        driverClassName: org.h2.Driver
        password: password
        url: jdbc:h2:mem:testdb
        username: sa
    h2:
        console:
            enabled: true
    jpa:
        database-platform: org.hibernate.dialect.H2Dialect

    config:
      import: configserver:http://localhost:9196

logging:
    file:
        name: C:/projects/springboot-microservices/spring-boot-microservices/order-service/src/main/resources/elk-stack.log

